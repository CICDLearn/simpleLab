- name: Configure Web Server
  hosts: all
  connection: docker # Connect directly to Docker container (no SSH needed)
  tasks:
    - name: Create a custom homepage
      copy:
        dest: /usr/share/nginx/html/index.html
        content: |
          <h1>Hello Staff!</h1>
          <p>This was deployed automatically.</p>